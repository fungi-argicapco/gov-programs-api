{
  "name": "@gov/web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run sync && bunx --bun vite dev",
    "build": "bun run sync && bunx --bun vite build",
    "preview": "bun run sync && bunx --bun vite preview",
    "sync": "bunx --bun svelte-kit sync"
  },
  "dependencies": {
    "@atlas/svelte": "workspace:*"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.43.8",
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "svelte": "^5.39.8",
    "vite": "^7.1.9"
  }
}
