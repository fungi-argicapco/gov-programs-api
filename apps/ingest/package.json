{
  "name": "@gov-programs/ingest",
  "version": "0.1.0",
  "description": "Scheduled ETL for government programs data ingestion",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "wrangler build",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest --run || true",
    "clean": "rm -rf dist .wrangler"
  },
  "dependencies": {
    "@gov-programs/common": "workspace:*",
    "@gov-programs/db": "workspace:*",
    "drizzle-orm": "^0.29.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "wrangler": "^3.24.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}